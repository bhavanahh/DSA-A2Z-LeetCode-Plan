Day1:

theory

What is Algorithmic Complexity?
Time Complexity → Why do we use Big-O?
Space Complexity → Extra memory used
Relation between loops & O(n), nested loops & O(n²)
Logarithmic operations → dividing by 2
Best Case, Worst Case, Average Case



 LeetCode Problems:

Add Two Integers (LC 2235)

int sum(int num1, int num2) {
    return num1 + num2;
}

Subtract Product and Sum of Digits (LC 1281)

#include <stdio.h>

int subtractProductSum(int n) {
    int sum = 0, product = 1;

    while (n > 0) {
        int digit = n % 10;
        sum += digit;
        product *= digit;
        n /= 10;
    }

    return product - sum;
}

int main() {
    int n;
    printf("Enter a number: ");
    scanf("%d", &n);

    int result = subtractProductSum(n);
    printf("Result = %d\n", result);

    return 0;
}


Day2:

theory

Prime check
GCD
Reverse digits


Leetcode Problem

Reverse Integer (LC 7)

#include <stdio.h>
#include <limits.h>

int reverse(int x) {
    long reversed = 0;
    while (x != 0) {
        int digit = x % 10;
        x /= 10;
        reversed = reversed * 10 + digit;
        if (reversed > INT_MAX || reversed < INT_MIN)
            return 0;
    }
    return (int)reversed;
}


Palindrome Number (LC 9)

#include <stdbool.h>
bool isPalindrome(int x) {
    if (x < 0 || (x % 10 == 0 && x != 0)) {
        return false;
    }
    int reversed = 0;
    
    while (x > reversed) {
        reversed = reversed * 10 + x % 10;
        x /= 10;
    }
    return (x == reversed || x == reversed / 10);
}


Day 03
Day 04
Running Sum (LC 1480)
int* runningSum(int* nums, int numsSize, int* returnSize) {
    for (int i = 1; i < numsSize; ++i) {
        nums[i] += nums[i - 1];
    }
    *returnSize = numsSize;
    return nums;
}

Pivot Index (LC 724)
int pivotIndex(int* arr, int n) {

    int arr_sum=0;
    for(int i=0;i<n;i++) arr_sum+=arr[i];

    int curr_sum=0, i=0;

    while(i<n)
    {
        if(curr_sum + arr[i] == arr_sum-curr_sum )
        {
            return i;
        }
        else
        {
            curr_sum+=arr[i];
        }
        i++;
    }    
    return -1;

}

Day 05

Leetcode problems:
1342 — Number of Steps to Reduce to Zero
int numberOfSteps(int num) {
    int steps = 0;
    
    while (num > 0) {
        if (num % 2 == 0) {
            num /= 2;     
        } else {
            num -= 1;      
        }
        steps++;
    }
    
    return steps;
}
191 — Number of 1 Bits
int hammingWeight(uint32_t n) {
    int count = 0;
    while (n != 0) {
        n = n & (n - 1);  
        count++;
    }
    return count;
}
